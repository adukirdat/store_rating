NODE_ENV=development
PORT=5000
DATABASE_URL=
JWT_SECRET=
CLIENT_URL=http://localhost:5173
# Production only: skipped outside NODE_ENV=production. Never commit a real password.
ADMIN_BOOTSTRAP_EMAIL=admin@example.com
ADMIN_BOOTSTRAP_PASSWORD=
# Required only when running the local development seed. Use a private local value.
SEED_DEMO_PASSWORD=
